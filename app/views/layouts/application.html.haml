!!!
%html
  %head
    %meta{:name => "viewport", :content => "width=device-width, initial-scale=1.0"}
    %title= content_for?(:title) ? yield(:title) : 'RnR at ThoughtWorks'
    %meta{:name => "description", :content => "#{content_for?(:description) ? yield(:description) : 'RnR at ThoughtWorks'}"}
    = stylesheet_link_tag "application", media: "all", "data-turbolinks-track" => true
    = javascript_include_tag "application", "data-turbolinks-track" => true
    = favicon_link_tag 'favicon.ico'
    = csrf_meta_tags
  %body

    .container.spacer
      %header.page-header
        %ul.nav.nav-pills.pull-right
          %li
            %p.navbar-text Hello #{@current_user}! (#{link_to 'Log out', logout_path, :class => "navbar-link"})
        %h1 RNR
      .row
        .col-md-4.col-md-offset-4
          = yield
    #footer
      .container
        %hr
        %p © 2014 ThoughtWorks, Inc.



